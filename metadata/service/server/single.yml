applications:
- elasticsearch
classes:
- service.elasticsearch.support
parameters:
  elasticsearch:
    server:
      enabled: true
      bind:
        address: 0.0.0.0
        port: 9200
      index:
        replicas: 0
      cluster:
        multicast: false
